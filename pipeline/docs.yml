version: 0.2
phases:
    install:
        runtime-versions:
            nodejs: 14

    build:
        commands:
            - cd ./docs
            - npm i -g rise-docs
            - rise-docs deploy
